<h1> Users </h1>

<% @users.each do |user| %>
  <ul id=<%= "user-#{user.id}" %>>
    <li><%= user.name %></li>
    <li><%= user.email %></li>
    <li><%= link_to 'Edit', edit_user_path(user.id)%></li>
</ul>
  <% end %>
